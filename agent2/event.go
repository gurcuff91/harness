package agent2

import "github.com/gurcuff91/harness/types"

// Type aliases — agent2 uses types.Event and types.Handler directly.
type Event = types.Event
type Handler = types.Handler
type EventType = types.EventType
